<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="registration">
        <h1>Registro</h1>
        
        <nav>
            <ul>
                <li><a href="{{ path('profile_index') }}">Perfil</a></li>
                <li><a href="{{ path('content_index') }}">Contenido</a></li>
                <li><a href="{{ path('marketplace_index') }}">Marketplace</a></li>
            </ul>
        </nav>

        <div class="registration-forms">
            {# Aquí puedes incluir el formulario de registro #}
            <form action="{{ path('registration_submit') }}" method="post">
                <label for="username">Nombre de usuario:</label>
                <input type="text" id="username" name="username" required>

                <label for="email">Correo electrónico:</label>
                <input type="email" id="email" name="email" required>

                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" required>

                <button type="submit">Registrarse</button>
            </form>
        </div>

        <footer>
            <p>&copy; 2023 Tu Empresa. Todos los derechos reservados.</p>
        </footer>
    </div>
</body>
</html>